# For execute a interactive mode in python you can run the script using flag -i
# example: python -i app.py

#Dir function whitout args return a name list of local scope currenty
# With Args return one list of valid attributes for the object. Example: dir()/dir(100)

## Help function invoke a system of helper interagrate. Is possible make searchs em interative mode or pass the param with the name, module function, class, method, or variable. 
# example: help() / help(100)